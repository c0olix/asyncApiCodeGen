package de.hochbahn.events;

import java.util.function.Consumer;
import org.springframework.messaging.Message;

public interface I{{.message.name}}Consumer {
	Consumer<Message<{{.message.name}}>> {{.operationId}}();
}